# React 19 trips several libraries' peer-dependency ranges. Recording it here means `npm install`
# and `npm ci` behave the same locally and in CI without anyone remembering the flag.
legacy-peer-deps=true
